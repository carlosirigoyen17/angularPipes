import { PasswordhidePipe } from './passwordhide.pipe';

describe('PasswordhidePipe', () => {
  it('create an instance', () => {
    const pipe = new PasswordhidePipe();
    expect(pipe).toBeTruthy();
  });
});
